# 設定再設計 v1

作成日: 2026-03-08

## 目的
- 機能追加のたびに増えてきた設定項目を棚卸しし、全体として一貫した情報設計へ整理する。
- `全体設定 / プロファイル別 / フォルダ別 / 一時UI状態` の保存スコープを分離し、どこに置くべき設定かを明確にする。
- 設定画面から運用操作を切り離し、利用者が「何を決める画面か」を理解しやすくする。

## 背景
- 現状は設定項目が段階的に追加されており、同種の設定でも配置先や保存単位が揃っていない。
- 一覧ヘッダーに常用の表示設定があり、設定モーダル側にも関連項目があるため、既定値と一時操作の境界が曖昧になっている。
- `useSettingsStore` と `useUIStore` の両方に近い責務の状態があり、実際には未使用または旧設計の名残になっている項目がある。

## 現状整理

### 現在の設定タブ
- 一般
- サムネイル
- スキャン
- 評価
- ストレージ
- 外部アプリ
- ログ
- バックアップ

### 現在の主な問題
- 設定と運用操作が混在している。
  - 例: 更新確認、ログ閲覧、バックアップ、ストレージ移行。
- 保存スコープが UI 上で明示されていない。
  - 例: `previewFrameCount` はプロファイル別、`thumbnailAction` は全体設定、フォルダ別スキャン設定は別ダイアログ。
- 常用する表示系設定がヘッダーに分散している。
  - 例: 並び替え、グループ化、サムネイル表示方式、表示プリセット。
- ストア責務が混在している。
  - `useSettingsStore`: 永続設定 + 旧設定 + 一部プロファイル文脈。
  - `useUIStore`: 一時UI状態 + 旧 `sortBy / sortOrder`。

### 整理対象として明示する項目
- `useSettingsStore.activeProfileId`
  - `useProfileStore.activeProfileId` と責務が重複している。
- `useSettingsStore.autoScanOnStartup`
  - フォルダ別設定へ寄せた後も残っている旧設定。
- `useSettingsStore.rightPanelVideoMuted`
  - ストアにはあるが、現時点で UI 導線と利用箇所が見えにくい。
- `useSettingsStore.cardLayout`
  - ストアにはあるが、現行 UI/描画では実効性が薄い。
- `useUIStore.sortBy / sortOrder`
  - 現行の一覧制御は `useSettingsStore` 側を使っており、旧状態の可能性が高い。

## 再設計の基本方針

### 1. 設定と運用操作を分離する
- 設定:
  - 既定の振る舞いを決めるもの。
- 運用操作:
  - 今この場で実行するもの。

設定画面に残すもの:
- 表示、再生、スキャン、保存先、連携先など。

設定画面から切り出す候補:
- 更新確認
- ログ閲覧 / コピー
- バックアップ作成 / CSV入出力
- ストレージ移行 / 旧データ削除

### 2. 保存スコープを UI 上で明示する
- 全体設定
  - アプリ全体に共通。
- プロファイル別
  - 現在のプロファイルだけに適用。
- フォルダ別
  - 登録フォルダごとに適用。
- 一時UI状態
  - 次回起動時まで持ち越さない、または設定画面で編集しない。

### 3. 「既定値」と「その場の操作」を分ける
- 設定画面:
  - 既定の表示モード
  - 既定の並び替え
  - 既定のグループ化
  - 既定の検索対象
- ヘッダー:
  - 今この一覧だけに対する一時変更

v1 ではまず「既定値の保存先」を整理し、ヘッダー UI は段階的に役割を縮退させる。

## 新しい情報設計（IA）案

### 1. 一般
- 起動時の初期画面
- 通知 / トースト方針
- パフォーマンスモード
- 言語や将来のテーマ設定

### 2. 一覧表示
- 既定の表示プリセット
- 既定のサムネイル表示方式
- 既定の並び替え
- 既定のグループ化
- 既定の検索対象（ファイル名 / フォルダ名）
- ファイルカード表示項目
  - ファイル名
  - 長さ
  - タグ
  - サイズ
- タグ表示ルール
  - ポップオーバー起点
  - 表示スタイル
  - 要約タグの並び順

### 3. プレビュー / 再生
- 動画音量
- 音声音量
- ライトボックス背景濃度
- 一覧ホバー時の動作
  - スクラブ
  - 自動パラパラ
  - 再生
- アニメ画像プレビュー
- Play モード詳細
- 右サイドバー動画プレビュー
  - 方式
  - ジャンプ間隔
  - ミュート方針

### 4. スキャン
- 対応形式カテゴリ
- プレビューフレーム数
- サムネイル解像度
- スキャン速度調整
- フォルダ別スキャン設定への導線
- 将来追加:
  - 除外拡張子
  - 除外フォルダ名
  - 隠し / システム除外
  - 最小サイズ除外

### 5. 保存 / キャッシュ
- 保存先モード
- カスタム保存先
- サムネイル整理
- 将来追加:
  - キャッシュ上限容量
  - 自動クリーンアップ
  - 空き容量しきい値

### 6. 連携
- 外部アプリ
- 拡張子ごとの既定アプリ
- 検索先管理
- 検索先インポート / エクスポート

### 7. 管理
- 更新確認
- ログ
- バックアップ
- CSV入出力
- ストレージ移行

`管理` は設定本体と分けて見せる。v1 では同じモーダル内に残してもよいが、ナビゲーション上は別区分として扱う。

## スコープ再定義

### 全体設定
- `thumbnailAction`
- `flipbookSpeed`
- `animatedImagePreviewMode`
- `rightPanelVideoPreviewMode`
- `rightPanelVideoJumpInterval`
- `videoVolume`
- `audioVolume`
- `lightboxOverlayOpacity`
- `performanceMode`
- `showFileName`
- `showDuration`
- `showTags`
- `showFileSize`
- `activeDisplayPresetId`
- `layoutPreset`
- `thumbnailPresentation`
- `sortBy`
- `sortOrder`
- `groupBy`
- `tagPopoverTrigger`
- `tagDisplayStyle`
- `fileCardTagOrderMode`
- `playMode`
- `externalApps`
- `defaultExternalApps`
- `searchDestinations`

### プロファイル別設定
- `fileTypeFilters`
- `previewFrameCount`
- `scanThrottleMs`
- `thumbnailResolution`

### フォルダ別設定
- 起動時スキャン
- 起動中新規ファイルスキャン
- フォルダ単位の対象カテゴリ

### 一時UI状態
- 設定モーダルの開閉
- 現在選択中タブ
- サイドバー幅
- 右パネル開閉
- スキャン進捗表示状態
- ヘッダー開閉状態

## 片付けるべき旧状態

### v1 で削除または移管したいもの
- `useSettingsStore.activeProfileId`
  - `useProfileStore` に一本化。
- `useSettingsStore.autoScanOnStartup`
  - フォルダ別設定移行済みのため削除候補。
- `useUIStore.sortBy / sortOrder`
  - 現在未参照なら削除候補。

### v1 で扱いを決めるもの
- `useSettingsStore.rightPanelVideoMuted`
  - UI へ正式昇格するか、未使用なら削除するか判断する。
- `useSettingsStore.cardLayout`
  - 現行デザインで使わないなら削除。将来使うなら「一覧表示」へ正式配置。

## 画面構成方針

### ナビゲーション
- 上段: 設定
  - 一般
  - 一覧表示
  - プレビュー / 再生
  - スキャン
  - 保存 / キャッシュ
  - 連携
- 下段: 管理
  - 更新
  - ログ
  - バックアップ / 入出力
  - データ移行

### 各セクションの共通ルール
- 冒頭に 1 行説明を置く。
- 各設定にスコープラベルを付ける。
  - `全体`
  - `プロファイル別`
  - `フォルダ別`
- セクション単位で `初期値に戻す` を置く。
- 低頻度の破壊的操作は通常設定と視覚的に分離する。

## 実装フェーズ案

### Step 1
- 設定項目棚卸しと情報設計を文書化する。
- 旧状態と未使用状態を明示する。

### Step 2
- ストア責務を整理する。
  - `useSettingsStore`: 永続設定のみ
  - `useProfileStore` / folder settings: 文脈依存設定
  - `useUIStore`: 一時UI状態のみ

### Step 3
- 設定画面のタブ構成を新 IA に合わせて再編する。
- 「設定」と「管理」を分離する。

### Step 4
- ヘッダーの表示系設定を「現在の一覧の一時変更」として再定義する。
- 既定値変更は設定画面からも操作可能にする。

### Step 5
- 除外ルール、自動クリーンアップ、設定のインポート / エクスポートなど未整備項目を追加する。

## 受け入れ条件
- 各設定に保存スコープが定義されている。
- 設定と運用操作の境界が UI 上で説明可能になっている。
- `useSettingsStore` / `useUIStore` の重複状態の削減対象が明文化されている。
- 新規機能追加時に「どのセクションへ置くか」「どのスコープへ保存するか」を判断できる基準がある。
