# 不具合・要望リスト

## このファイルの役割
- 未解決の不具合・要望を管理する
- 解決した内容は `CHANGELOG.md` に反映し、ここから整理する（このファイルは `docs/dev/` 配下）

## 推奨フォーマット
- 優先度: 高 / 中 / 低
- 状態: 未着手 / 調査中 / 対応中 / 解決済み
- 備考: 再現条件・回避策

## 運用メモ

> 修正完了したらチェックマークを付けること。
> 修正完了しているもので時間が経った物は削除（１週間程度目安）
---

## 🐛 不具合

### 緊急度: 高
(なし)

### 緊急度: 中
- [x] `ffmpeg Worker / Utility Process 化` と `スキャン段階反映` の試作を積むと、一覧表示・ウィンドウ拡大・右クリックメニュー表示が全体的にもたつくことがある
  - 優先度: 中
  - 状態: 解決済み（2026-03-05 確認）
  - 備考: `preview frame` / `video thumbnail` / `duration` / `metadata` の個別 Worker 化、軽量な段階反映、再スキャン開始待ち短縮、スキャン中DB参照軽量化の反映後、現時点で未再現。旧試作のように一覧以外まで広く再読込する実装は引き続き避ける

### 緊急度: 低
(なし)
---

## ⚡ パフォーマンス改善候補

- `ffmpeg Worker / Utility Process 化` 再挑戦時は、UI 応答性の計測ポイントと開発フラグを先に入れて退行検知しやすくする

---

## 💡 要望

### 未対応
#### UI関係
- ファイルカードの表示方法の統一、詳細表示モードに合わせたものにする

#### システム関係
- スマートフォルダ機能（ルールビルダー型 + 保存検索型）
  - 優先度: 中
  - 状態: 対応中
  - 備考: v1 は「現在の絞り込み条件保存」「Sidebar からの再適用」に加えて、対象範囲・検索語・タグ条件・評価条件を編集できる条件ダイアログを導入。条件ネストや自動アクションは対象外。

### 対応済み（削除待ち）
#### UI関係
- [x] ヘッダー2段目を開閉できるようにしたい
  - 優先度: 中
  - 状態: 解決済み（2026-03-07 実装）
  - 備考: 1段目に「表示設定」ボタンを配置し、表示設定パネルを開閉できるようにした。タイプ絞り込みは検索欄の隣へ移し常時操作できる配置へ変更。
  - 受け入れ条件: 1段目の表示設定ボタンで該当パネルが開閉し、タイプ絞り込みは1段目から直接操作できる。
- [x] 上部ヘッダーの操作が詰まって見えるので整理したい
  - 優先度: 中
  - 状態: 解決済み（2026-03-07 実装）
  - 備考: ヘッダーを2段レイアウト化し、タイプ絞り込みは「初回クリックで単独表示→単独選択中に別タイプを押すと追加→2件以上はトグル」の流れへ調整（Ctrl+クリックでも複数選択可能）。
  - 受け入れ条件: 右パネル表示時など横幅が狭い状態でもヘッダー操作が読める形で表示される。
- [x] ファイルタイプ（動画/画像/書庫/音声）で一覧を絞り込みたい
  - 優先度: 中
  - 状態: 解決済み（2026-03-07 実装）
  - 備考: 一覧ヘッダーにタイプ絞り込みトグルを追加。タグ/評価/検索語と併用して絞り込み可能で、単独/複数選択を段階的に切り替えられる。
  - 受け入れ条件: 初回クリックで単独表示、単独選択中の別タイプクリックで条件追加、2件以上選択時はON/OFF切り替えできる。
- [x] 漫画版の詳細表示を追加（詳細表示のサムネイルを縦長、情報エリアを細めにしたバージョン）
  - 優先度: 中
  - 状態: 解決済み（2026-03-07 実装）
  - 備考: 表示モードに `詳細表示（漫画）` を追加し、`サムネイル左（2:3） / 情報右` の横長カードを実装。
  - 受け入れ条件: `詳細表示（漫画）` で縦長サムネイルと縮小情報エリアが崩れず表示される。
- [x] 書庫ファイルの画像数を一覧カード上で確認したい
  - 優先度: 中
  - 状態: 解決済み（2026-03-07 互換修正反映）
  - 備考: 書庫メタデータの `imageEntries` 件数を FileCard の右下バッジ（動画の再生時間と同形式）で表示。旧メタデータ形式（`imageCount` / `pageCount` / `fileCount`）でも件数を読み取れるよう互換フォールバックを追加し、書庫メタデータ不足の既存ファイルは再スキャン時に再取得するよう修正。複数画像がある書庫のみ `NNp` を表示。
  - 受け入れ条件: 書庫で画像が2枚以上ある場合、一覧カード右下に `NNp` が表示される。
- [x] リリース版でUIが表示されないことがある
  - 優先度: 高
  - 状態: 解決済み（2026-03-07 d6/d7 修正）
  - 備考: リリース配布物でレンダラー成果物（`dist`）が参照できずUIが表示されないケースに対し、配布物への同梱を明示し、起動時の読み込み先フォールバックとエラーログを追加。あわせて d7 で起動前段フォールバックログ（`%TEMP%/MediaArchiver-v2-startup.log`）を追加し、ログ未生成時の切り分けを可能にした。
  - 受け入れ条件: リリースZIPを展開して起動したとき、メインUIが表示される。
- [x] 右サイドバーのタグ編集モードを明確化（常時削除ボタン表示の誤操作リスクを下げる）
  - 優先度: 高
  - 状態: 解決済み（2026-03-06 確認）
  - 備考: 通常時はタグ表示のみとし、編集モード時のみタグの追加・削除操作を表示する構成へ変更。
  - 受け入れ条件: 通常時は削除ボタンが表示されず、編集モード時のみ削除操作が表示される。
- [x] FileCard の伸縮表示を調整（中央寄せ時の余白が目立つ問題を緩和する）
  - 優先度: 中
  - 状態: 解決済み（2026-03-06 確認）
  - 備考: FileGrid の行配置を左揃え固定に変更し、詳細表示ではカード幅へ余白を再配分して右側の空白を抑えるよう調整。
  - 受け入れ条件: 代表的な表示モードで、中央寄せ時の余白が過剰に見えない。
