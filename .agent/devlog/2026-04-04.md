# 2026-04-04

## 実施内容
- Issue #20（特定のプロファイルで起動）を実装・リリース
- `--profile <名前>` / `--profile=<名前>` 引数で起動時プロファイルを指定できるようにした
- `databaseManager.ts` に `getProfileByName()` を追加し SQL WHERE 句で名前検索を最適化
- v1.13.0 としてリリース（ビルド・タグ・プッシュ完了）
- Issue #20 をコミット時点でクローズ

## メモ
- 今後はコミット完了時点で関連 Issue をクローズする運用に変更
- ビルド初回に vite の並列ビルド競合エラーが出ることがある（再実行で解消）
