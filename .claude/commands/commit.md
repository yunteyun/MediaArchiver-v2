現在の変更をコミットしてください。

.agent/workflows/commit.md および AGENTS.md のコミット規約に従い、以下を実施してください：

1. git status と git diff で変更内容を確認する
2. 変更内容を分析してコミットメッセージの種別（[機能追加] / [改善] / [修正] / [整理]）を判断する
3. `git add .` は使わず、対象ファイルを個別に git add する
4. 以下の形式でコミットする：
   ```
   [種別] 変更内容の要約（日本語・動詞で締める）

   - 変更点1
   - 変更点2
   ```
5. git push origin main でプッシュする

コミット前にユーザーへコミットメッセージ案を提示し、承認を得てから実行すること。
