## [1.9.1] - 2026-03-13
### Added
- (なし)

### Changed
- ファイル右クリックの移動メニュー文言を整理し、移動先を選ぶ操作と登録フォルダへすぐ移動する操作の違いを分かりやすくした。
- バージョン命名規則を見直し、修正のみのリリースでも `d` は使わず `PATCH` を上げる運用へ統一した。

### Fixed
- 重複チェックの類似候補検索で、数字付きの付加情報を含むファイル名や、作品名中の数字だけが違うファイルでも候補判定が安定するようにした。
- ファイル一覧の Shift 範囲選択で、グループ表示時に見た目より多く選ばれてしまうことがある問題を修正した。
- 左サイドバーとファイル移動先のツリーで、ファイルが入っていない子孫フォルダも表示して移動先に選べるようにした。
- 名前変更ダイアログを開いた直後は、拡張子を除いたファイル名部分だけが選択されるようにした。
