## [1.10.3] - 2026-03-21
### Added
- (なし)

### Changed
- 動画の `再開 / 見どころ` まわりの並び順保持と内部処理を共通化し、将来の調整で画面ごとの差が出にくいよう整理した（表示への変更なし）。
- 使われていない旧ライトボックス実装を整理し、表示設定とメディア判定の共通部分をまとめて保守しやすくした（表示への変更なし）。
- 現行導線に使っていない旧ライトボックス部品をさらに整理し、中央ビューアのキーボード操作ルールを共通 helper で管理する形へ寄せた（表示への変更なし）。
- 中央ビューアの書庫単体表示では `Esc` と左右キーをページ移動優先で扱うよう見直し、戻り方を迷いにくくした。
- 右パネルのタグ編集欄は補足文を外し、操作ボタンまわりをすっきり見渡せるようにした。

### Fixed
- 開発用スクリプトを含めて lint が通る状態へ戻し、中央ビューアで Hook 順序違反が起きうる実装を修正した。
- ライトボックスと中央ビューアで、タグ取得や書庫プレビュー読み込みに失敗した際の案内を利用者に分かる形へそろえた。
- 中央ビューアで動画表示中に左右キーが前後ファイル移動と競合しやすい問題を抑えた。
- 配布ビルドで起動エントリが梱包先と合わず ZIP 版の生成が止まる問題を修正した。
