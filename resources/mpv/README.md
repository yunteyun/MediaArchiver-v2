# mpv バイナリ配置ディレクトリ

このディレクトリに `mpv.exe` を配置してください。

## 入手先

https://mpv.io/installation/ から Windows 版をダウンロードし、
`mpv.exe` をこのディレクトリに配置してください。

## 動作確認済みバージョン

mpv 0.38.0 以降を推奨します。

## mpv が見つからない場合

`resources/mpv/mpv.exe` が存在しない場合、動画再生は HTML5 `<video>` 要素に自動フォールバックします。
